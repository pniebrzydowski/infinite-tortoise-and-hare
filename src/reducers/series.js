export const initialState = {
    series: null,
};

const series = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default series;
